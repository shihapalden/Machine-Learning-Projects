# -*- coding: utf-8 -*-
"""Diabetes Prediction using Support Vector Classification

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1GA2Sl8F94_OFktiOmjM6FZovbwjLM6qA
"""

import pandas as pd
from sklearn.svm import SVC
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score

data = pd.read_csv('diabetes_data.csv')
x = data.drop(['Has Diabetes','ID'], axis=1)
y = data['Has Diabetes']

X_train, X_test, Y_train, Y_test = train_test_split(x, y, test_size=0.2, random_state=42)

model = SVC(kernel='linear')
model.fit(X_train, Y_train)

pred = model.predict(X_test)
accuracy = accuracy_score(Y_test, pred)
print(f"Accuracy score is: {accuracy}")

new = pd.DataFrame({
    'Age': [40],             # Age in years
    'BMI': [20.5],          # Body Mass Index
    'Blood Pressure': [110], # Systolic Blood Pressure
    'Glucose Level': [60],  # Glucose level in mg/dL
    'Insulin Level': [130]     # Insulin level in μU/mL
})

prediction = model.predict(new)
print(prediction)
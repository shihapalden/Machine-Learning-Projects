# -*- coding: utf-8 -*-
"""Student Marks Prediction Using Ridge(L2) and Lasso(L1) Regression

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1HfNVgTkGr_bnIeUT_Pbwj5fSPiZkN9bD
"""

import pandas as pd
from sklearn.linear_model import Ridge, Lasso
from sklearn.model_selection import  train_test_split
from sklearn.metrics import mean_squared_error, r2_score

data = pd.read_excel('studentmarks.xlsx')
x = data['Studied Hours']
y = data['Marks']
x = x.values.reshape(-1,1) #Becouse redge & lasso expect 2D

X_train, X_test, Y_train, Y_test = train_test_split(x,y, test_size=0.2, random_state=42)

ridge = Ridge(alpha=0.1)
ridge.fit(X_train, Y_train)

#Finding the best alpha value
'''for alpha in [0.01, 0.1, 1]:
  ridge = Ridge(alpha=alpha)
  ridge.fit(X_train, Y_train)
  predictions = ridge.predict(X_test)
  mse = mean_squared_error(Y_test, predictions)
  print(f"Ridge with alpha={alpha}: R^2 Score:", ridge.score(X_test, Y_test))
  print(f"Mean Squared Error with alpha={alpha}: {mse}")'''

new = pd.DataFrame({
    'Studied Hours' : [7.25]
})

prediction = ridge.predict(new)
print(prediction)

lasso = Lasso(alpha=0.1)
lasso.fit(X_train, Y_train)

new = pd.DataFrame({
    'Studied Hours' : [7.25]
})

prediction = lasso.predict(new)
print(prediction)

'''for alpha in [0.01, 0.1, 1]:
  lasso = Lasso(alpha=alpha)
  lasso.fit(X_train, Y_train)
  predictions = lasso.predict(X_test)
  mse = mean_squared_error(Y_test, predictions)
  print(f"Lasso with alpha={alpha}: ", r2_score(X_test, predictions))
  print(f"Mse with alpha={alpha}:", mse)'''